<h1>Edit Agent</h1>

<%= form_for @agent do |f| %>
    <%= f.file_field :avatar %>

    <table>
    <tr>
      <th>Name</th>
      <td><%= f.text_field(:name) %></td>
    </tr>
    </table>

    <%= submit_tag("Update Agent") %>
 <% end %>

 <%= link_to 'Back to list', {:action => 'index'} %>